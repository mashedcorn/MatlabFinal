function fight()
Damage=0

    m = mobiledev;
    m.AccelerationSensorEnabled = 1;
    m.Logging = 1;
    [a,t] = accellog(m);
    pause(5);
    
    disp(a);
    Damage = int8(sqrt(a(1,1)^2+a(1,2)^2+a(1,3)^2 )-1);


